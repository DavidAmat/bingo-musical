<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=1122, initial-scale=1"/>
  <title>{{ sheet_title }}</title>
  <style>
    /* A4 landscape: 297mm x 210mm = 1122px x 794px at 96dpi */
    @page { 
      size: A4 landscape; 
      margin: 0; 
    }
    
    * { box-sizing: border-box; }
    
    html, body { 
      margin: 0;
      padding: 0;
      width: 1122px;
      height: 794px;
      overflow: hidden;
    }
    
    body {
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif;
      background: #f3efe4;
      color: #1a1a1a;
    }
    
    .sheet {
      width: 1122px;
      height: 794px;
      padding: 15px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      grid-template-rows: 1fr 1fr;
      gap: 15px;
    }
    
    .cell {
      background: #700302;
      border-radius: 8px;
      padding: 12px;
      overflow: hidden;
    }
    
    .bingo-card {
      background: transparent;
      width: 100%;
      height: 100%;
      display: flex;
      flex-direction: column;
    }
    
    .card-header {
      display: flex;
      align-items: center;
      justify-content: flex-start;
      margin-bottom: 8px;
      position: relative;
    }
    
    .title-wrap { 
      display: flex; 
      align-items: center; 
    }
    
    .card-title {
      font-weight: 800;
      letter-spacing: 0.5px;
      font-size: 18px;
      text-transform: uppercase;
      margin: 0;
      line-height: 1.2;
      color: #fff;
      white-space: nowrap;
    }
    
    .card-id {
      position: absolute;
      left: 0;
      bottom: -14px;
      font-weight: 700;
      font-size: 11px;
      color: #fff;
      opacity: 0.8;
    }
    
    .grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      grid-template-rows: repeat(2, 1fr);
      gap: 8px;
      flex: 1;
      margin-top: 10px;
    }
    
    .song-card {
      background: rgba(255, 253, 246, 0.92);
      border: 1px solid #d1b07a;
      border-radius: 6px;
      padding: 8px 6px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: stretch;
      min-width: 0;
      min-height: 0;
    }
    
    .song-title {
      font-weight: 800;
      font-size: 13px;
      line-height: 1.2;
      margin-bottom: 3px;
      text-align: center;
      width: 100%;
      word-wrap: break-word;
      overflow-wrap: break-word;
    }
    
    .song-artist {
      font-weight: 500;
      font-size: 11px;
      opacity: 0.85;
      line-height: 1.2;
      text-align: center;
      width: 100%;
      word-wrap: break-word;
      overflow-wrap: break-word;
    }
    
    @media print {
      html, body {
        width: 297mm;
        height: 210mm;
      }
      .sheet { 
        width: 297mm;
        height: 210mm;
        page-break-after: always;
      }
    }
  </style>
</head>
<body>
  <div class="sheet">
    {% for card in cards %}
      <div class="cell">
        {% with title=card.title, card_number=card.card_number, background_color=card.background_color, songs=card.songs, title_images=card.title_images %}
          {% include 'card_inner.html.j2' %}
        {% endwith %}
      </div>
    {% endfor %}
  </div>
</body>
</html>
