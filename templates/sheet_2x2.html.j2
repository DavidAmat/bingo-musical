<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=1122, initial-scale=1"/>
  <title>{{ sheet_title }}</title>
  <style>
    /* A4 landscape: 297mm x 210mm = 1122px x 794px at 96dpi */
    @page { 
      size: A4 landscape; 
      margin: 0; 
    }
    
    * { box-sizing: border-box; }
    
    html, body { 
      margin: 0;
      padding: 0;
      width: 1122px;
      height: 794px;
      overflow: hidden;
    }
    
    body {
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif;
      background: #f3efe4;
      color: #1a1a1a;
    }
    
    .sheet {
      width: 1122px;
      height: 794px;
      padding: 15px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      grid-template-rows: 1fr 1fr;
      gap: 15px;
    }
    
    .cell {
      background: #700302;
      border-radius: 8px;
      padding: 12px;
      overflow: hidden;
    }
    
    /* Card-specific styles - using same font sizes as card.html.j2 */
    .bingo-card {
      background: transparent;
      width: 100%;
      height: 100%;
      padding: 0;
      display: flex;
      flex-direction: column;
    }
    
    .card-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 8px;
    }
    
    .title-wrap { 
      display: flex; 
      align-items: center; 
    }
    
    .card-title {
      font-weight: 800;
      letter-spacing: 0.5px;
      font-size: 18px; /* scaled for sheet */
      text-transform: uppercase;
      color: #fff;
      white-space: nowrap;
      margin: 0;
      line-height: 1.2;
    }
    
    .card-id {
      font-weight: 700;
      font-size: 20px; /* scaled for sheet */
      color: #fff;
      opacity: 0.8;
    }
    
    .grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      grid-template-rows: repeat(2, 1fr);
      gap: 6px; /* scaled for sheet */
      flex: 1;
      margin-top: 8px;
    }
    
    .song-card {
      background: rgba(255, 253, 246, 0.92);
      border: 1px solid #d1b07a; /* scaled for sheet */
      border-radius: 5px; /* scaled for sheet */
      padding: 5px 4px; /* scaled for sheet */
      display: block;
      overflow: hidden;
    }
    
    .song-card-inner {
      display: table;
      width: 100%;
      height: 100%;
      table-layout: fixed;
    }
    
    .song-card-cell {
      display: table-cell;
      vertical-align: middle;
      text-align: center;
    }
    
    /* IMPORTANT: These font sizes match card.html.j2 and will scale proportionally */
    .song-title {
      display: block;
      font-weight: 800;
      font-size: 19px; /* SAME as card.html.j2 */
      line-height: 1.2;
      margin-bottom: 8px; /* SAME as card.html.j2 */
      text-align: center;
      word-wrap: break-word;
      overflow-wrap: break-word;
    }
    
    .song-artist {
      display: block;
      font-weight: 500;
      font-size: 18px; /* SAME as card.html.j2 */
      opacity: 0.85;
      line-height: 1.2;
      text-align: center;
      word-wrap: break-word;
      overflow-wrap: break-word;
    }

    @media print {
      html, body {
        width: 297mm;
        height: 210mm;
      }
      .sheet { 
        width: 297mm;
        height: 210mm;
        page-break-after: always;
      }
    }
  </style>
</head>
<body>
  <div class="sheet">
    {% for card in cards %}
      <div class="cell">
        {% with title=card.title, card_number=card.card_number, background_color=card.background_color, songs=card.songs, title_images=card.title_images %}
          {% include 'card_inner.html.j2' %}
        {% endwith %}
      </div>
    {% endfor %}
  </div>
</body>
</html>
